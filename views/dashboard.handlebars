         <nav class="bg-gray-800 p-4">
            <div class="flex items-center">
                <a href="/" class="text-white text-xl font-bold mr-4">Home</a>
                <a class="text-white text-xl font-bold mr-4">Home</a>
    {{#if loggedIn}}
      <a id="logout" class="text-white text-xl mr-4 ">Posts</a>
     <a href="/logout" id="logout"  class="text-white text-xl">Sign Out</a>
      
    {{else}}
      <a href="/login" class="text-white text-xl mr-4">Login</a>
    {{/if}}
  </div>
</nav>

<section>
    <div>
        <div class="container has-text-centered">
            <h1 class="title">Dashboard</h1>
        </div>
    </div>
</section>

<div class="container">
    <section class="articles">
         <div class="container grid grid-cols-1 md:grid-cols-3 gap-4">
        {{#each posts as |post|}}
        <div class="bg-white p-4 rounded shadow-md">
            <h3 class="text-xl font-semibold mb-2"><a href="./dashboard/{{id}}"{{post.title}} class="text-cyan-600 hover:underline">{{title}}</a></h3>
            <p class="text-gray-700 mb-4">{{content}}</p>
            <p class="text-gray-600 font-semibold">User ID: {{user_id}}</p>
        </div>
        {{/each}}
    </div> 
        </div>
    </section>
</div>



<script src="./js/login.js"></script>
<script src="./js/logout.js"></script>
<script src="./js/modal.js"></script>